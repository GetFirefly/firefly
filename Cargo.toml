[workspace]

members = [
  "examples/spawn-chain",
  "lumen",
  "lumen_runtime",
  "liblumen_arena",
  "liblumen_common",
  "liblumen_diagnostics",
  "liblumen_syntax",
  "liblumen_core",
  "liblumen_beam",
  "liblumen_compiler",
  "liblumen_codegen",
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
lto = true
