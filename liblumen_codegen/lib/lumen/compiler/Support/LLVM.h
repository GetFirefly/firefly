#ifndef LUMEN_SUPPORT_LLVM_H
#define LUMEN_SUPPORT_LLVM_H

//  For now we just use the same support system as MLIR,
// but we may add more forward declarations as needed
#include "mlir/Support/LLVM.h"

#endif
